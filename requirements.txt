flask==3.0.3
flask-socketio==5.3.6
flask-cors==4.0.1
mediapipe==0.10.14
# OpenCV: HGRIA's static path uses only core cv2 APIs (VideoCapture, color
# convert, Laplacian, CLAHE, imencode/imdecode). MediaPipe 0.10.14 requires
# opencv-contrib-python (unpinned by MediaPipe). Both packages export `cv2`;
# installing them at different versions makes `import cv2` ambiguous.
# Pin BOTH to 4.10.0.84 for a clean install. Do not also install opencv-python.
# Do not leave opencv-contrib-python unpinned (it will float and shadow cv2).
opencv-python-headless==4.10.0.84
opencv-contrib-python==4.10.0.84
numpy==1.26.4
pytest==8.3.2
pytest-cov==5.0.0
freezegun==1.5.1
hypothesis==6.112.0
pyngrok==7.2.0
python-socketio==5.11.2
python-engineio==4.9.1
# Dynamic gesture dependencies
onnxruntime==1.17.1
filterpy==1.4.5
scipy==1.15.3
